spring:
  application:
    name: compute-service
server:
  port: 8081
eureka:
  client:
     serviceUrl:
     defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
  instance:
    hostname: localhost
